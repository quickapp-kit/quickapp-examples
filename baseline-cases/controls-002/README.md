# Controls-002

B2 baseline for a numeric Slider and a text Picker. The range is a local
pipe-delimited list (`Low|Medium|High`) and `selected` is its zero-based index.

Build with `node scripts/build-controls.mjs`.
