# List-001

B3 baseline for one authoritative Runtime Tree with an explicit Scroll and
List Host container, a keyed `for` list, local images, and scroll lifecycle
handlers. It does not implement virtualization or a second list tree.

Build with `node scripts/build-list.mjs`.
