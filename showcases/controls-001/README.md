# Controls-001

Public B1 control contract baseline. It uses only local alliance DSL and
contains a text input plus a boolean switch. Native platform mapping is
validated by platform agents.

Build with `node scripts/build-controls.mjs`.
