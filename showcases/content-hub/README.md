# Content Hub

Content Hub is a compact daily-list and content-card showcase. It uses only
the existing alliance DSL, three keyed cards, deterministic manual refresh,
and the shared Home/Detail navigation path.

Build with `node scripts/build-content-hub.mjs`. The generated RPK and metadata
are written to `dist/`.
