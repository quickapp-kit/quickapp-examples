# Wearable-001

Wearable-001 is a compact today-summary and health-task showcase. Its
content stays inside a 220x220 safe area and uses only short rows, small local
images, deterministic refresh, and the existing Home/Detail route contract.

Build with `node scripts/build-wearable.mjs`. The generated RPK and metadata
are written to `dist/`.
