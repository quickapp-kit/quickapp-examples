# Health Summary

Health Summary is a compact today-summary and health-task showcase. Its
content stays inside a 220x220 safe area and uses a real Scroll -> List path,
keyed rows, short text, small local images, deterministic refresh, and the
existing Home/Detail route contract.

Build with `node scripts/build-health-summary.mjs`. The generated RPK and metadata
are written to `dist/`.
